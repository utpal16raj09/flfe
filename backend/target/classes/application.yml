server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:flfe;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console
  mail:
    host: smtp.gmail.com
    port: 587
    username: subhasisbhuyan2004@gmail.com # replace with the Gmail/Workspace sender
    password: nxxx ltaz apqd oyoo # use a Google App Password (not your normal password)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${app.oauth2.google.client-id}
            client-secret: ${app.oauth2.google.client-secret}
            scope:
              - openid
              - profile
              - email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          google:
            issuer-uri: https://accounts.google.com

app:
  jwt:
    # generate a base64 secret, e.g. openssl rand -base64 32
    secret: qKByUHo9cJgN0j62RURkoXJ9HQ0+S5xXx9rlXz7VUO4=
    expirySeconds: 86400
  oauth2:
    redirectUri: http://localhost:3000/oauth/callback
    google:
      client-id: 1037287206276-67d2u6oionkd1q9s1skrg3j27s99n2r9.apps.googleusercontent.com
      client-secret: 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
  product:
    name: FLFE
